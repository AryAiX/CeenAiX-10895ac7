logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.env
.cursor/mcp.json
.vercel
.env*.local
docs/keys.local.md

# Generated SQL chunks/bundles (scripts/split-migrations-for-remote.mjs)
supabase/.temp/migration-chunk-*.sql
supabase/.temp/bundle-*.sql

# UI parity pass scratch space (scripts/ui-parity-servers.sh + ui-parity-capture.mjs).
# Dev-server pids, logs, and the ephemeral playwright cache are never committed.
.ui-parity/
.reference/

# UI parity screenshot artifacts. We version-control the directory
# structure via .gitkeep / README files, but the PNG baselines change with
# every port pass and are regenerated on demand.
docs/ui-parity/**/*.png
